# Git
.git
.gitignore
.github

# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
*.egg-info
dist
build
*.egg

# Virtual environments
venv
env
ENV
.venv

# Testing
.pytest_cache
.coverage
htmlcov
.tox
.hypothesis

# IDE
.vscode
.idea
*.swp
*.swo
*~
.DS_Store

# Environment
.env
.env.local
.env.*.local
credentials.json
state.json

# Documentation
docs/
*.md
!README.md

# Tests
tests/
pytest.ini

# Debug logs (will be created at runtime)
debug_logs/

# Manual test script
manual_api_test.py

# CI/CD
.clabot

# Cache (e.g. trivy.db from security scans)
.cache/

# Frontend dependencies (installed inside Docker)
webui/node_modules
webui/.next

# 9router's node_modules is installed inside Dockerfile.ninerouter. The host copy
# carries glibc native binaries (@next/swc-*-gnu, lightningcss-*-gnu,
# better-sqlite3) while the build image is alpine/musl — copying it over the
# container's install breaks the build.
9router/node_modules
9router/.next

# Misc
*.log
.crush
