# Prod dependencies
fastapi
uvicorn[standard]
httpx
loguru
python-dotenv
tiktoken
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
alembic>=1.13
python-jose[cryptography]>=3.3
bcrypt>=4.0
cryptography>=42.0
python-multipart>=0.0.9
google-auth>=2.0.0
# NOTE: the PII guardrail (kiro/guardrails/) has no third-party dependency.
# presidio-anonymizer was used and then dropped — its AnonymizerEngine is
# quadratic in span count (25 -> 800 spans cost 0.73ms -> 225ms on a fixed
# text) and its DeanonymizeEngine cannot restore a custom operator's output at
# all. presidio-analyzer was never an option: spaCy plus a ~600MB English
# model, seconds per full-history payload, and no help with Vietnamese names.

# Testing dependencies
pytest
pytest-asyncio
hypothesis